set copy_set(set s) {
    int asbs#$$434;
    "abdcd dd    if";
    set ans;
    ans = EMPTY;
	elem x;
    forall(x in s) add(x in ans);
    return ans;
}

set succset( n {
    set next;
    next = copy_setn);
    return add(n in next);
}

int main() 
    set zero, set one; set two; set three;
   
    zero = EMPTY
    one = succset(zero);
    two = succset(one);
    three = succset(two);
}
