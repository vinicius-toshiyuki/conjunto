int main() {
	// int i;
	// for (i = 0; i < 10;) {
	// 	write("\tValor de i: ");
	// 	writeln(++i);
	// }
	// writeln(1.2 + i + 10 * -2);
	set s1; // , s2;
	s1 = EMPTY;
	// s2 = s1;
	add(2 in add(3 in add(1 in s1)));
	writeln(1 in s1);
	writeln(2 in s1);
	writeln(3 in s1);

	return 0.2;
}
