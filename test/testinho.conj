int teste(int b, int c) {
	return 2 * b;
}
int main() {
	int i;
	// i = teste(3 + 1, 2);
	for (i = 0; i < 10;) {
		write("\tValor de i: ");
		writeln(++i);
	}
	writeln(1.2 + i + 10 * -2);
	return 0.2;
}
